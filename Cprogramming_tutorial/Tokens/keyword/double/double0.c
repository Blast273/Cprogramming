#include <stdio.h>


int main() {

	/* double : is a type of fix size of 8 bytes 
	 * double : double fix size of 8 bytes is 64 bits in binary (1's and 0's)*/

	auto double age = 1000000;

	printf("age : %f\n", age); 
	printf("age : %.2f\n", age);
	return 0;


}
